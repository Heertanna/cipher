# Railway/Nixpacks: monorepo has Vite + Express; force Node API install + start
# (prevents Railpack from auto-selecting static-site + Caddy for the Vite app.)
[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci"]

[start]
cmd = "npm run start"
